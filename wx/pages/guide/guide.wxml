<view class="page">
  <view class="bg-grid"></view>

  <view wx:if="{{skill}}" class="content">
    <view class="header-card">
      <view class="brand-icon" style="border-color: {{skill.brandColor}};">
        <text class="brand-emoji">{{skill.iconEmoji}}</text>
      </view>
      <view class="header-info">
        <text class="brand-name">{{skill.brand}}</text>
        <view class="strategy-tag" style="background: {{skill.brandColor}}33; border-color: {{skill.brandColor}}66; color: {{skill.brandColor}};">
          获胜策略
        </view>
        <text class="strategy-text">{{skill.strategy}}</text>
      </view>
    </view>

    <view class="prompt-card">
      <view class="prompt-header">
        <text class="prompt-title">Master Prompt</text>
        <button class="copy-btn" bindtap="copyPrompt">📋 一键复制</button>
      </view>
      <scroll-view class="prompt-scroll" scroll-y="true">
        <view class="prompt-text">
          <rich-text nodes="{{promptNodes}}"></rich-text>
        </view>
      </scroll-view>
    </view>

    <view class="action-bar">
      <button class="action-btn pulse" bindtap="handleEnterTool">
        进入 {{skill.brand}} 开始实战
      </button>
    </view>
  </view>
</view>
